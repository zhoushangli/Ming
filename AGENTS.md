## 项目环境

1. Godot 源码路径为 `C:\Work\Godot\Editor\godot`
2. 构建项目时优先参考 `.vscode/tasks.json`

## 文件修改规则

1. 不要回滚用户已有修改，除非用户明确要求。
2. 修改范围应尽量小，只改和任务直接相关的代码。
3. 非必要不要新增类，代码应该保持简洁。
4. 优先修改已有函数和已有数据结构，除非现有职责明显不适合。
5. 不要为了通用性提前设计复杂接口，先满足当前项目需求。

## 代码风格

1. 优先遵循项目已有写法、命名、目录结构和调用方式。
2. 代码应保持简洁，避免过度抽象。
3. 不要为了单个需求引入复杂框架或额外层级。
4. 新增逻辑应尽量放在已有职责相近的文件中。

## 注释规范

注释分为两类。

### 流程类注释

用于说明执行步骤，格式使用：

```cpp
// 1) Initialize state
// 2) Check conditions
// 3) Apply results
```

### 效果类注释

用于说明函数、变量或行为的实际效果，必须两行：

```cpp
// Load texture from the given path and return a texture object.
// e.g. LoadTexture("res://Textures/Board.png")
```

## 构建与验证

1. 构建前先查看 `.vscode/tasks.json`。
2. 优先使用项目已有构建任务。
3. 修改 C++ 代码后，应尽量执行对应构建或最小验证。
4. 如果无法构建，需要说明原因和未验证风险。

## Agent 工作方式

1. 开始任务前先阅读相关代码，不要直接猜实现。
2. 如果没有明确说明执行修改，则先向我提问，讨论需求矛盾处，明确后再执行修改。
3. 涉及 Godot 行为或兼容性时，可读取 `C:\Work\Godot\Editor\godot`。
4. 完成后简要说明修改了哪些文件、行为变化以及是否构建或验证。
